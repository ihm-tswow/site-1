name: CI
on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
jobs:
  build-api:
    runs-on: ubuntu-20.04
    steps:
      # Build
      - uses: actions/checkout@v2
      - name: Create dummy commit file
        run: touch __dummy.txt
      - name: Commit build files
        run: git add . && git config user.name test && git config user.email test && git commit -m "Bump"
      - name: Push
        run: git push "https://ihm-tswow:${{secrets.ACCESS_TOKEN}}@github.com/ihm-tswow/site-2.git" -f HEAD:main